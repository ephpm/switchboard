[package]
name = "switchboard"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
description = "GitHub webhook handler for ePHPm preview deployments"

[dependencies]
# HTTP server (receives webhooks)
axum = "0.8"
tokio = { version = "1", features = ["full"] }

# HTTP client (GitHub API)
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# YAML (ephpm.yaml app manifest + switchboard secrets file). serde_yml is the
# actively-maintained fork of the archived serde_yaml — chosen to avoid the
# RUSTSEC-2024-0320 "unmaintained" advisory if switchboard is open-sourced.
serde_yml = "0.0.12"

# Crypto (webhook signature verification)
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Error handling
anyhow = "1"
thiserror = "2"

# CLI
clap = { version = "4", features = ["derive", "env"] }
base64 = "0.22"

[dev-dependencies]
tempfile = "3"
